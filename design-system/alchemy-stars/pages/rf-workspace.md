# RF arms workspace — 1.3.0-rf.7

The current desktop shell and screenshot-led editor rules override generic marketing-page suggestions. The design-system search returned landing-page patterns twice, so those patterns, fonts and colors are not adopted.

- Primary task: configure reference-based RF hand adaptation, generate, inspect the actual output and open interchange files.
- Place RF with animation blend, model parts and dual merge in the left workspace navigation, not in preferences. Retain existing Ctrl+1…5 mappings; RF uses Ctrl+6.
- Reuse semantic theme brushes, native inputs, themed vector icons and flat dock panels. No new palette, decorative dashboard metrics or sample model pretending to be a result.
- A 420 DIP independently scrolling configuration pane groups sources/reference, grip fitting, output scope, units/axes and advanced left/right adjustment. The result pane consumes the remaining width; the existing compact navigation rail handles 900×600.
- One primary Generate action remains visible below both panes. Ctrl+E dispatches RF generation only on the RF page. Empty input explains the missing reference/source; busy state prevents duplicate export. Completion remains inline, with separate quality-review messaging.
- The right pane displays an actual generated bitmap, or an explicit empty/unavailable state. It is a generation snapshot, never a live simulation. FBX, blend, preview and report each have working open actions; project replacement clears/disposes the old result.
- Verify Chinese/English at 900×600 and 1460×900, light/dark, navigation/shortcut routing, empty/busy/complete/failure states, preserved settings and numeric editability.
